#PBS -N PyGCN_CoraFull
#PBS -l walltime=4:00:00
#PBS -l nodes=1:gpus=1
#PBS -l mem=64GB
#PBS -j oe
#PBS -A PAS1069

comment=valid_part_4
workspace=mini_batch_large_scale

userHome=/users/PAS1069/osu8206
workshop=${userHome}/GCN
# srcFilePath=${workshop}/${workspace}/src


cd $TMPDIR

# cp ${srcFilePath}/*.py .
cp $PBS_O_WORKDIR/module/*.py .
cp $PBS_O_WORKDIR/*.py .

singularity exec --nv $HOME/Dockers/gpu-pytorch_geometric-v7.sif python exec_code.py

cp -avr ./results $PBS_O_WORKDIR/results_${comment}

cd $PBS_O_WORKDIR
